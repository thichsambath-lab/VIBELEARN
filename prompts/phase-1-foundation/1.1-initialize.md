# Implementation Prompt: Phase 1 - Foundation (Repo Setup)

## 1. Goal
Initialize both `frontend` (React + Vite + Tailwind CSS) and `backend` (Node.js + Express) workspaces following the specifications in `docs/system_design/frontend.md` and `docs/system_design/backend.md`, configure ESLint and Prettier, set up `.env.example`, and verify that both development servers launch and pass linting cleanly.

## 2. Skills & References Read
- `AGENTS.md` (Workflow rules, approval gates, technology lock, checking rules)
- `roadmap.md` (Phase 1 tasks: 1.1 through 1.7)
- `docs/system_design/frontend.md` (React Vite, Tailwind CSS, folder hierarchy, JavaScript only)
- `docs/system_design/backend.md` (Express layered architecture, folder hierarchy, JavaScript only)
- `docs/system_design/architecture.md` (Decoupled architecture, no TypeScript, no containerization)
- `docs/prd.md` (Scope, auth flow, learning features, video embeds)

## 3. Code & Environment Inspected
- Current workspace root: `c:\Users\CADT\OneDrive\Desktop\docs`
- Node.js runtime: v24.18.0
- npm version: 11.16.0
- Existing files: `AGENTS.md`, `roadmap.md`, `prd.md`, `seed.ndjson`, `videos.json`, `design/`, `system_design/`
- Workspace state: `frontend/` and `backend/` directories do not exist yet.

## 4. Decisions and Assumptions
- **Strict JavaScript Only**: No TypeScript in frontend or backend as mandated by `architecture.md`.
- **Frontend Setup**:
  - Vite with React template (`vite` + `@vitejs/plugin-react`).
  - Tailwind CSS v3 with PostCSS and Autoprefixer (`tailwindcss`, `postcss`, `autoprefixer`).
  - Folder structure strictly adhering to `docs/system_design/frontend.md` (`src/assets`, `src/components`, `src/pages`, `src/hooks`, `src/services`, `src/utils`).
  - Minimal placeholder `App.jsx` with Tailwind sanity check styling and `main.jsx` entry point.
- **Backend Setup**:
  - Node.js project initialized with `"type": "module"` for ES Modules syntax.
  - Express.js with `cors`, `dotenv`, and `express.json()` middleware.
  - Folder structure strictly adhering to `docs/system_design/backend.md` (`src/config`, `src/db`, `src/middlewares`, `src/routes`, `src/controllers`, `src/services`, `src/repositories`).
  - `src/index.js` setting up the server, CORS, JSON parsing, a health check endpoint `GET /api/health`, centralized 404 handler, and error handler.
- **Code Quality & Tooling**:
  - ESLint 9+ flat config in both frontend (`frontend/eslint.config.js`) and backend (`backend/eslint.config.js`).
  - Prettier configuration (`.prettierrc`, `.prettierignore`) in both workspaces.
  - Consistent formatting and clean scripts: `dev`, `start`, `lint`.
- **Environment Variables**:
  - `frontend/.env.example`: `VITE_CLERK_PUBLISHABLE_KEY`, `VITE_API_BASE_URL`.
  - `backend/.env.example`: `PORT`, `NODE_ENV`, `DATABASE_URL`, `CLERK_SECRET_KEY`, `CLERK_PUBLISHABLE_KEY`, `FRONTEND_URL`.

## 5. Files to Create & Touch
- `frontend/package.json`
- `frontend/vite.config.js`
- `frontend/index.html`
- `frontend/tailwind.config.js`
- `frontend/postcss.config.js`
- `frontend/.env.example`
- `frontend/.prettierrc`
- `frontend/eslint.config.js`
- `frontend/src/main.jsx`
- `frontend/src/App.jsx`
- `frontend/src/index.css`
- `frontend/src/components/.gitkeep`
- `frontend/src/pages/.gitkeep`
- `frontend/src/hooks/.gitkeep`
- `frontend/src/services/.gitkeep`
- `frontend/src/utils/.gitkeep`
- `frontend/src/assets/.gitkeep`
- `backend/package.json`
- `backend/.env.example`
- `backend/.prettierrc`
- `backend/eslint.config.js`
- `backend/src/index.js`
- `backend/src/config/index.js`
- `backend/src/middlewares/errorHandler.js`
- `backend/src/db/.gitkeep`
- `backend/src/routes/.gitkeep`
- `backend/src/controllers/.gitkeep`
- `backend/src/services/.gitkeep`
- `backend/src/repositories/.gitkeep`

## 6. Requirements
1. **Task 1.1**: Initialize `frontend` directory with Vite, React, standard JavaScript.
2. **Task 1.2**: Initialize `backend` directory with Node.js, Express, standard JavaScript.
3. **Task 1.3**: Configure TailwindCSS in `frontend`, importing Tailwind directives in `src/index.css`.
4. **Task 1.4**: Configure ESLint and Prettier across both `frontend` and `backend` workspaces.
5. **Task 1.5**: Create `.env.example` templates in both `frontend` and `backend`.
6. **Task 1.6**: Verify `npm run dev` and `npm run lint` succeed in both workspaces.
7. **Task 1.7**: Checkpoint verification and update `roadmap.md`.

## 7. Security Considerations
- Keep all secrets in `.env` (gitignored). Only commit `.env.example` containing non-secret placeholder keys.
- Frontend must never include database credentials or Clerk secret key.
- CORS on backend must restrict or configure origins using `FRONTEND_URL`.
- Express backend must sanitize JSON payloads and handle errors centrally without leaking internal stack traces in production.

## 8. Acceptance Criteria
- [x] `frontend/` directory exists with Vite + React + Tailwind CSS properly configured.
- [x] `backend/` directory exists with Express layered architecture folders and base server in `src/index.js`.
- [x] `npm run lint` passes with zero errors in `frontend/`.
- [x] `npm run lint` passes with zero errors in `backend/`.
- [x] `npm run build` passes in `frontend/`.
- [x] `GET /api/health` returns status 200 `{ "status": "ok" }` when backend is running.
- [x] Both `.env.example` files match the architecture specifications.
- [x] `roadmap.md` is updated upon successful completion.

## 9. Checks to Run
- In `frontend/`: `npm install`, `npm run lint`, `npm run build`.
- In `backend/`: `npm install`, `npm run lint`.
- Test launch development servers to verify clean boot.

## 10. Manual Test Steps
1. Navigate to `frontend/` and run `npm run dev`. Verify Vite server boots on localhost.
2. Open the frontend in browser (or curl) and verify the Tailwind-styled placeholder renders without console errors.
3. Navigate to `backend/` and run `npm run dev` (or `node src/index.js`).
4. Issue a request to `http://localhost:5000/api/health` and verify `{ "status": "ok", "timestamp": "..." }` response.
5. Verify linting passes in both folders: `cd frontend && npm run lint` and `cd backend && npm run lint`.
